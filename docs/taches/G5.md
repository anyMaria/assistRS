# Tâche G5 — Mesurer+ : objectifs, temps, rapports PDF, créneaux auto, import CSV

## Avant de commencer (obligatoire)

- Lis `docs/CONCEPTION.md` §7 (Pôle Mesurer) et `docs/GUIDELINE.md` §4 bloc « G5 »
  (§364+), §8 (Vercel Blob) et §10 (PDF — `@react-pdf/renderer`).
- Regarde l'état réel du code : `git log --oneline -15`, `src/db/schema.ts`,
  `src/lib/kpi.ts` (déjà existant, à réutiliser pour le rapport). Ne recasse rien de
  G1-G4.
- Modèle recommandé : **Sonnet** (pas Opus).
- Note : la connexion Meta automatique (Instagram/Facebook) est un bloc **séparé**,
  `G5bis.md` — à faire juste après ce bloc-ci, pas en même temps. L'import CSV construit
  ici reste utile pour LinkedIn et les cas d'erreur de sync Meta.

## Objectif de ce bloc

Objectifs par marque, saisie de temps passé, rapport PDF mensuel, créneaux personnalisés
auto-calculés, et import CSV pour les stats saisies hors de l'app.

## Ce qu'il faut construire

- `goals`, `time_entries` (schéma `CONCEPTION.md` §10) : CRUD simples (objectifs dans
  `/marques/[id]`, temps via le bouton « + » global et depuis une publication).
  Progression des objectifs affichée sur l'accueil et dans `/analyse`.
- Import CSV (`/statistiques` → « Importer un CSV ») : upload → `papaparse` côté serveur
  → aperçu 5 lignes → écran de mapping (colonne CSV → champ relevé via `STAT_FIELDS`) →
  « Enregistrer ce mapping » (`csv_mappings`, déjà en base) → association des lignes aux
  publications (par URL exacte, sinon date+plateforme, sinon création à la volée avec le
  titre du CSV) → insertion des relevés + rapport d'import (X importés, Y ignorés).
- Croisement horaires dans `/analyse` : tableau « tes heures réelles vs recommandées »
  (agrégat des relevés par jour/heure de publication). Si ≥ 10 publications avec stats
  sur une plateforme : encart « Mettre à jour ta grille ? » → aperçu des changements →
  application = upsert `time_slots` source `personnalise`.
- Rapport PDF : installer `@react-pdf/renderer`. `GET /api/rapports/[accountId]/[mois]`
  → document : page de garde (nom marque, mois, sa couleur), KPI vs mois précédent, top 3
  publications, objectifs, temps passé (valorisé si taux horaire), pied de page
  « Généré avec Assist RS ». Stocké dans Blob + listé dans `/mesurer`. Section
  « Recommandations » : bouton IA optionnel (prompt `GUIDELINE.md` §5.6, réutilise le
  client Gemini de G2).

## Critères d'acceptation

- [ ] Import d'un CSV de test (fabrique un fichier proche du format Buffer) crée les
      bons relevés et le mapping est réutilisable pour un import suivant.
- [ ] Un PDF se génère avec des vraies données et se télécharge.
- [ ] La proposition de créneaux n'apparaît qu'à partir de 10 publications avec stats.
- [ ] Saisie de temps possible en moins de 10 secondes depuis mobile (bouton +).
- [ ] `npx tsc --noEmit` et `npm run build` passent, blocs précédents intacts.

## Fin de bloc (obligatoire)

- Un commit local par fonctionnalité terminée, message en français. **Ne pousse pas.**
- Résume ce qui a été fait et tout point ambigu (notamment si la section IA du rapport
  a pu être testée avec une vraie clé Gemini).
- **Arrête-toi ici.** N'enchaîne pas sur G5bis : Ana doit valider et ajuster d'abord.
